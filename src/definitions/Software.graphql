type Software {
   creationDate: LocalDateTime!
   creator: String!
   isDebug: Boolean!
   isILOnly: Boolean!
   isNative: Boolean!
   name: String!
   shortName: String!
   targetFramework: String!
   targetProcessor: String!
   version: String
}

extend type Query {
  Software: [Software]
}

